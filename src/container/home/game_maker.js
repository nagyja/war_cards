//dependent on state set by selected mode, draw a number of cards from each deck, 
//display the results allowing the user to flip through cards; swipe left or right to advance to next card, swipe up to discard a card

import React, { Component } from 'react';

class GameMaker extends Component {
    render(){
        return(
             <div>
               GameMaker
            </div>
        );
    }
}

export default GameMaker;