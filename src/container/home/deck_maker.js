//displays a list of all available cards with check boxes, a form field for deck name, on submit save the card as a deck.
import React, { Component } from 'react';


class DeckMaker extends Component {
    render(){
        return(
             <div>
               DeckMaker
            </div>
        );
    }
}

export default DeckMaker;